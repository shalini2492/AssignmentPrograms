package DataStructureprograms;
import java.io.FileNotFoundException;
import java.util.ArrayList;

import Utility.Utility;
public class UnorderedList {
public static void main(String[] args) throws Exception
{
	Utility utility = new Utility();
	UnorderedLinkedList list1=new UnorderedLinkedList();
	Utility.readListFile("binarytextfile.txt");			
	System.out.println("Enter string which you wants to remove : ");
	String key = utility.inputString();
	if(list1.searchInList(key))
	{
		System.out.println("Element is present in list");
	}
	else
	{
		System.out.println("Element is not present in list");
	}
	}
	
}
	/*String w;
	LinkLIst<String> l = new LinkLIst<String>();
	Utility utility= new Utility();
	String file = "/home/hp/Desktop/abcd.txt";
	String readfile = l.readfromFile(file);
	
	String[] noarr=readfile.split(" ");
	for(int i=0; i<noarr.length; i++)
	{
         l.add(noarr[i]);
	}
	l.display();
	System.out.println("Elements in linked list are:");
    l.display();
	System.out.println("Enter the word to search in file");
	w=utility.inputString();
	l.searchItem(null, w);
	for(int i=0; i<noarr.length; i++)
	{
		if(noarr[i] == w)
		{
			l.remove(w, i);
			System.out.println("Element at position "+i+ " is deleted.");
		}
		else
			l.add(w);
		    System.out.println("Element "+w+ " is inserted.");
		    l.display();
	}
}

*/
}
