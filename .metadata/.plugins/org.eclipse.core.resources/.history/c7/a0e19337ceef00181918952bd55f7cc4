package DataStructureprograms;
import java.io.FileNotFoundException;
import java.util.ArrayList;

import Utility.Utility;
public class UnorderedList {
public static void main(String[] args) throws Exception
{
	StringNode head;
	public void insert(String words)
	{
		StringNode node1 = new StringNode(words, head);
		node1.data = words;
		node1.next = null;
		if(head == null)
		{
			head = node1;
		}
		else
		{
			StringNode node2 = head;
			while(node2.next!=null)
			{
				node2 = node2.next;
			}
			node2.next = node1;
		}
	}
	public void show() 
	{
		StringNode node = head;
		while(node.next!=null)
		{
			System.out.print(node.data+ " --> ");
			node = node.next;
		}
		System.out.println(node.data);
	}	
	
	public void remove(String str) 
	{		
	   StringNode temp = head;
	// Store head node 
	   StringNode prev = null; 
	 
	  // If head node itself holds the key to be deleted 
	   if (temp != null && temp.data == str) 
	   { 
	       head = temp.next; // Changed head 
	        return; 
	   } 
	   while (temp != null && temp.data != str) 
	   { 
	       prev = temp; 
	       temp = temp.next; 
	   }     
	 
	   if (temp == null)
	       	return;  
	   prev.next = temp.next;	   
	}
	public boolean searchInList(String key) 
	{	// TODO Auto-generated method stub
		
		if(head==null)
		{
			return false;
		}
		StringNode current = head;
		while(current!=null)
		{
			if(current.data == key)
			{
				return true;
			}
			current = current.next;
		}			
		return true;
	}
}
	/*String w;
	LinkLIst<String> l = new LinkLIst<String>();
	Utility utility= new Utility();
	String file = "/home/hp/Desktop/abcd.txt";
	String readfile = l.readfromFile(file);
	
	String[] noarr=readfile.split(" ");
	for(int i=0; i<noarr.length; i++)
	{
         l.add(noarr[i]);
	}
	l.display();
	System.out.println("Elements in linked list are:");
    l.display();
	System.out.println("Enter the word to search in file");
	w=utility.inputString();
	l.searchItem(null, w);
	for(int i=0; i<noarr.length; i++)
	{
		if(noarr[i] == w)
		{
			l.remove(w, i);
			System.out.println("Element at position "+i+ " is deleted.");
		}
		else
			l.add(w);
		    System.out.println("Element "+w+ " is inserted.");
		    l.display();
	}
}

*/
}
